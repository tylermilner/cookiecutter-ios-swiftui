[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# No non-dev packages currently

[dev-packages]
cookiecutter = "==2.*"
pytest = "==8.*"
pytest-cookies = "==0.7.*"
pyyaml = "==6.*"
types-PyYAML = "==6.*"
ruff = "==0.4.*"
mypy = "==1.10.*"
pre-commit = "*"

[requires]
python_full_version = "3.12.2"
